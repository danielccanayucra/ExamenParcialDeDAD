server:
  port: 2011
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/danielccanayucra/ExamenParcialDeDAD.git # ruta del repositorio git
          searchPaths: config-data
          default-label: main